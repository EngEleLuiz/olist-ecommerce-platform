# Dashboard-only requirements — stripped of everything not needed at runtime
python-dotenv==1.0.1
loguru==0.7.2
pandas==2.2.2
numpy==1.26.4
pyarrow==15.0.2

# ML — only what the dashboard actually runs
scikit-learn==1.5.0
lightgbm==4.3.0
lifetimes==0.11.3
duckdb==1.1.3
statsmodels==0.14.2

# Dashboard
streamlit==1.35.0
plotly==5.22.0
